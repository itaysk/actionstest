
name: test
on:
  workflow_dispatch:
  push:
jobs:
  xxx:
    name: xxx
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Start Tracee
        uses: itaysk/tracee-action@805ed53e36e354868d4f98c7b72dc2d38c8ac081

      - name: do something
        run: ping google.com -c 1 || touch foo
        
      - name: Stop Tracee
        uses: itaysk/tracee-action@a61a6a1609d90a877fa5d06f9b9755e6c9d35f05
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          fail-on-diff: "false"
          create-pr: "true"
