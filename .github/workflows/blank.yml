
name: test
on:
  workflow_dispatch:
  push:
jobs:
  xxx:
    name: xxx
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Start Tracee
        uses: itaysk/tracee-action@7eec0310870884d2b3411c44c4f8882dcfb2a3b5

      - name: do something
        run: ping google.com -c 1 || touch foo
        
      - name: Stop Tracee
        uses: itaysk/tracee-action@b8f68131d2db78ee73826e8ef4ca89c5c8aa2cdc
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          fail-on-diff: "false"
          create-pr: "true"
