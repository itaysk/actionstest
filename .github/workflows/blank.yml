
name: test
on:
  workflow_dispatch:
  push:
jobs:
  xxx:
    name: xxx
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Start Tracee
        uses: itaysk/tracee-action@95ef2286dc3a9d7219f39191f2de1fb1d4effaa9

      - name: do something
        run: ping google.com -c 1 || true
        
      - name: Stop Tracee
        uses: itaysk/tracee-action@4c016c7c706a6dc26f93a7a84c53ae046e1699c7
        with:
          fail-on-diff: "true"
          create-pr: "true"
