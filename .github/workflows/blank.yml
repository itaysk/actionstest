
name: test
on:
  workflow_dispatch:
  push:
jobs:
  xxx:
    name: xxx
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Start Tracee
        uses: itaysk/tracee-action@7eec0310870884d2b3411c44c4f8882dcfb2a3b5

      - name: do something
        run: ping google.com -c 1 || touch foo
        
      - name: Stop Tracee
        uses: itaysk/tracee-action@4c016c7c706a6dc26f93a7a84c53ae046e1699c7
        with:
          fail-on-diff: "true"
          create-pr: "true"
